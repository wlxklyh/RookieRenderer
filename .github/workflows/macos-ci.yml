name: macOS
on: push
jobs:
  build:
    runs-on: macos-latest
    steps:
    - name: Checkout
      uses: actions/checkout@master
    - name: GotoProjDir
      run: cd Proj
    - name: Build
      run: ./build_macos.sh
      
